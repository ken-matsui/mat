let foo: i32;

fn main(bar: i32) -> void {
    let baz: i32 = qux;
    foo = bar;
    let qux: i32;
    if quux {
        bar = garply;
        baz = qux;
        let garply: i32;
    }
    let grault: i32;
    let garply: i32;
    let grault: i32;
}
